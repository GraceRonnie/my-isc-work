import numpy as np

arr = np.zeros((2, 3, 4)) #gives two separate arrays of 0 in 3 lines by 4 rows
print arr 

arr = np.ones((2, 3, 4))
print arr

arr = np.arange(1000) #just gives a full array 0 to 999
print arr